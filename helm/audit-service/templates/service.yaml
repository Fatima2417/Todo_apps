apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-service.fullname" . }}
  labels:
    app: {{ include "audit-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ include "audit-service.name" . }}
